# AutoGen AgentChat stack (pyautogen 0.10.x is a proxy package).
# NOTE: Old code used `import autogen` from legacy `pyautogen` 0.2.x.
# This repo targets the modern stack: `autogen_agentchat` + `autogen_ext`.
pyautogen>=0.10,<0.11
autogen-ext[openai]>=0.4,<0.8
openai>=1.0.0
datasets>=2.14.0
huggingface-hub>=0.19.0
sentence-transformers>=2.2.2
numpy>=1.24.0
tqdm>=4.65.0
python-dotenv>=1.0.0
